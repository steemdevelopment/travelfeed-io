import React, { Fragment } from "react";
import Layout from "../components/Layout.js";
import { Typography } from "@material-ui/core";

const About = () => (
  <Fragment>
    <Layout>
      <Typography variant="display1">Tag</Typography>
      <Typography>A tag view will be added soon</Typography>
    </Layout>
  </Fragment>
);

export default About;
